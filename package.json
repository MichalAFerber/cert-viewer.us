{
  "name": "cert-web-viewer",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file cert viewer (static, no build step). This package.json exists only for the CI harness tooling.",
  "scripts": {
    "test": "node test/harness.mjs",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.10.0",
    "globals": "^17.12.0",
    "playwright": "^1.56.1"
  },
  "type": "module"
}
